# النشر على chef-hashem.com

الدومين مضبوط في المستودع مسبقاً. ما تبقّى خطوتان تنفّذهما أنت: تفعيل الاستضافة،
وضبط سجلات الـ DNS عند مزوّد الدومين.

> `chef-hashem.com` بحروف صغيرة — أسماء النطاقات لا تفرّق بين الكبير والصغير،
> وGitHub Pages يشترط الصيغة الصغيرة في ملف `CNAME`.

## ما هو جاهز في المستودع

| الملف | الغرض |
| --- | --- |
| `CNAME` | يخبر GitHub Pages أن يخدم الموقع على `chef-hashem.com` |
| `robots.txt` | يسمح بالأرشفة ويشير إلى خريطة الموقع |
| `sitemap.xml` | خريطة الموقع |
| `index.html` | الرابط الأساسي `canonical` وبطاقات المشاركة ووسم `url` في البيانات المنظّمة |

مجلد `/manage/` غير مذكور في `robots.txt` عمداً — ذكره فيه يكشف مساره للجميع.
الصفحة نفسها تحمل `noindex, nofollow`.

---

## 1. تفعيل GitHub Pages

الفرع الحالي هو فرع تطوير، وGitHub Pages ينشر من الفرع الرئيسي. لذلك:

1. ادمج طلب السحب (Pull Request) إلى `main`.
2. **Settings → Pages**
3. في **Source** اختر `Deploy from a branch`، ثم `main` و `/ (root)`، واحفظ.
4. سيظهر حقل **Custom domain** وفيه `chef-hashem.com` مأخوذاً من ملف `CNAME`.

## 2. سجلات الـ DNS

عند مزوّد الدومين (Namecheap، GoDaddy، Cloudflare…) أضف:

**للدومين الجذر** — أربعة سجلات `A`:

```
A   @   185.199.108.153
A   @   185.199.109.153
A   @   185.199.110.153
A   @   185.199.111.153
```

**وللـ www** — سجل `CNAME` واحد:

```
CNAME   www   amer1973tubishat-wq.github.io
```

> إن كان الدومين على Cloudflare، اجعل السجلات الأربعة **DNS only** (السحابة رمادية)
> إلى أن يصدر GitHub شهادة HTTPS، ثم فعّل الوكيل إن أردت.

## 3. تفعيل HTTPS

بعد انتشار الـ DNS (غالباً أقل من ساعة، وقد يصل إلى 24 ساعة)، ارجع إلى
**Settings → Pages** وفعّل **Enforce HTTPS**.

النتيجة:

- الموقع: `https://chef-hashem.com`
- لوحة الإدارة: `https://chef-hashem.com/manage/`

---

## بدائل الاستضافة

نفس الملفات تعمل كما هي:

- **Netlify:** استورد المستودع، واترك أمر البناء فارغاً ومجلد النشر `.`، ثم
  **Domain settings → Add a custom domain**.
- **Cloudflare Pages:** **Workers & Pages → Create → Pages**، أمر البناء فارغ
  ومجلد الإخراج `/`، ثم **Custom domains**.

في كلا الحالتين احذف ملف `CNAME` أو تجاهله — فهو خاص بـ GitHub Pages.

---

## حماية `/manage/` حماية حقيقية

كلمة المرور داخل اللوحة تعمل في متصفح الزائر، فهي تمنع الفتح العابر ولا تحمي شيئاً.
أي شخص يفتح ملفات الموقع يستطيع قراءتها. للحماية الفعلية:

- **Cloudflare Zero Trust** (مجاني حتى 50 مستخدماً): **Access → Applications**،
  أضف تطبيقاً على `chef-hashem.com/manage/*` واسمح لبريدك فقط. هذا أفضل خيار مع
  GitHub Pages، لأن Pages وحده لا يدعم كلمات مرور المجلدات.
- **Netlify:** حماية بكلمة مرور من إعدادات الموقع، أو Netlify Identity.
- **استضافة cPanel:** خاصية **Directory Privacy** على مجلد `manage`.

خطوة إضافية: غيّر اسم المجلد من `manage` إلى اسم لا يُخمَّن. يقلّل الفضول، وليس بديلاً
عمّا سبق.

---

## نشر تعديلات القائمة

اللوحة تحفظ في متصفحك أنت فقط. لتصل التعديلات إلى الزوّار:

1. عدّل `assets/data/menu.js`.
2. شغّل `node tools/build.mjs` لتحديث نسخة القائمة الثابتة داخل `index.html`.
3. ادفع التغيير إلى `main` — تنشره الاستضافة تلقائياً.

## قبل الإطلاق

- استبدل الهاتف `+966 55 123 4567` ورقم واتساب والعنوان من **لوحة التحكم → معلومات المطعم**
- غيّر كلمة مرور اللوحة من تبويب **كلمة المرور**
- استبدل `og:image` بصورة JPG أو PNG — أغلب مواقع التواصل لا تعرض SVG في بطاقات المشاركة
- ضع `/manage/` خلف حماية حقيقية كما في الأعلى

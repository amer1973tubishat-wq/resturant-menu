# النشر والدومين الخاص · Deploying with a custom domain

الموقع ملفات ثابتة فقط، فيعمل على أي استضافة. هذه الخطوات لأشهر ثلاث خيارات مجانية.

> **ملاحظة مهمة قبل البدء:** لا أستطيع شراء الدومين أو الدخول إلى حسابك في الاستضافة —
> هذه خطوات تنفّذها أنت. أخبرني بالدومين الذي تملكه وسأضيف ملف الإعداد المناسب للمستودع.

---

## 1. GitHub Pages (الأسهل، والمستودع موجود على GitHub أصلاً)

1. من صفحة المستودع: **Settings → Pages**
2. في **Source** اختر `Deploy from a branch`، ثم الفرع `main` والمجلد `/ (root)`
3. احفظ. خلال دقيقة سيعمل الموقع على:
   `https://amer1973tubishat-wq.github.io/resturant-menu/`

### ربط دومين خاص

1. اشترِ الدومين من أي مسجّل (Namecheap، GoDaddy، Cloudflare، إلخ).
2. في **Settings → Pages → Custom domain** اكتب الدومين، مثلاً `chefhashem.com`، واحفظ.
   هذا ينشئ ملف `CNAME` في المستودع تلقائياً.
3. عند مزوّد الدومين أضف هذه السجلات:

   **للدومين الجذر** (`chefhashem.com`) — أربعة سجلات `A`:

   ```
   A   @   185.199.108.153
   A   @   185.199.109.153
   A   @   185.199.110.153
   A   @   185.199.111.153
   ```

   **وللنطاق الفرعي** (`www.chefhashem.com`) — سجل `CNAME` واحد:

   ```
   CNAME   www   amer1973tubishat-wq.github.io
   ```

4. ارجع إلى **Settings → Pages** وفعّل **Enforce HTTPS** بعد أن يتحقق الدومين
   (قد يستغرق انتشار الـ DNS حتى 24 ساعة، وغالباً أقل من ساعة).

بعدها:

- الموقع: `https://chefhashem.com`
- لوحة التحكم: `https://chefhashem.com/manage/`

---

## 2. Netlify

1. `netlify.com` → **Add new site → Import an existing project** → اختر المستودع.
2. اترك أمر البناء فارغاً، ومجلد النشر `.` (الجذر).
3. **Domain settings → Add a custom domain** ثم اتبع سجلات الـ DNS التي يعرضها.

يمنحك Netlify شهادة HTTPS تلقائياً، وأيضاً **حماية حقيقية لمجلد الإدارة** — انظر أسفل.

## 3. Cloudflare Pages

1. `dash.cloudflare.com` → **Workers & Pages → Create → Pages** → اربط المستودع.
2. أمر البناء: اتركه فارغاً. مجلد الإخراج: `/`.
3. **Custom domains → Set up a domain**. إن كان الدومين مسجّلاً في Cloudflare فالربط تلقائي.

---

## حماية مجلد الإدارة حماية حقيقية

كلمة المرور داخل اللوحة تمنع الفتح العابر فقط. الصفحة تُنزَّل كاملة إلى متصفح الزائر،
فمن يبحث يستطيع قراءتها. للحماية الفعلية استخدم الاستضافة:

- **Netlify:** أضف ملف `_headers` أو فعّل **Password protection** من إعدادات الموقع
  (متاح في الخطط المدفوعة)، أو استخدم **Netlify Identity**.
- **Cloudflare:** **Zero Trust → Access → Applications**، وأضف تطبيقاً على المسار
  `/manage/*` يسمح لبريدك الإلكتروني فقط. متاح مجاناً حتى 50 مستخدماً.
- **استضافة عادية (cPanel):** استخدم **Directory Privacy** على مجلد `manage`،
  وهو ما ينشئ `.htaccess` + `.htpasswd`.

خطوة إضافية بسيطة: غيّر اسم المجلد من `manage` إلى اسم لا يُخمَّن، وحدّث الرابط عندك.
هذا يقلل الفضول، لكنه ليس بديلاً عن الحماية أعلاه.

---

## كيف تصل تعديلات اللوحة إلى الزوّار؟

اللوحة تحفظ في متصفحك أنت (`localStorage`)، فتظهر التعديلات لك فوراً عند فتح الموقع
على نفس الجهاز والمتصفح — **ولا تظهر لبقية الزوّار**.

لنشرها للجميع، عدّل ملف البيانات في المستودع:

1. افتح `assets/data/menu.js` وعدّل الأصناف أو الأسعار.
2. شغّل `node tools/build.mjs` لتحديث نسخة القائمة الثابتة داخل `index.html`.
3. ارفع التغيير إلى GitHub — تنشره الاستضافة تلقائياً.

> إن أردت أن يَنشُر زرُّ الحفظ في اللوحة مباشرةً للجميع، فذلك يحتاج خادماً أو نظام
> إدارة محتوى، وليس موقعاً ثابتاً. أخبرني إن أردت هذا المسار.

# Local environment — never commit API keys
.env
.env.local
.mcp.local.json

# Python artifacts from skill scripts
__pycache__/
*.pyc
.coverage

# Playwright, installed on demand for tests/save-flow (see its README)
node_modules/
